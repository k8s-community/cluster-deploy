apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus
    chart: prometheus-3.1.0
    component: "server"
    heritage: Tiller
    release: my-release
  name: my-release-prometheus-server
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 9090
  selector:
    app: prometheus
    component: "server"
    release: my-release
  type: "ClusterIP"